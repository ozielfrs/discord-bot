const {
	token,
	cliID,
	blockedGuildIDs,
	blockedUserIDs,
	GuildIDs,
} = require(`../../private/conf/config.json`)

module.exports = {
	token,
	cliID,
	blockedGuildIDs,
	blockedUserIDs,
	GuildIDs,
}
